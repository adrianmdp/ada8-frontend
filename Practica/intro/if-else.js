
// IF e IF ELSE

if(condición) {

    // código 

} else if(otra condición) {

    // código

} else if(otra condición más) {

    // Código

} else {

    // código
    
}

// Si la condición es true entonces se ejecuta lo que está entre las llaves que le siguen

// Para que se evalúe la segunda conidción. La primera tiene que haber dado false.